{
  "services": [
    {
      "name": "backend",
      "source": {
        "path": "."
      },
      "dockerfile": {
        "path": "Dockerfile",
        "dockerfile": "Dockerfile"
      },
      "env": {
        "SECRET_KEY": "${{secrets.SECRET_KEY}}",
        "DATABASE_URL": "${{postgresql.DATABASE_URL}}"
      }
    }
  ]
}
